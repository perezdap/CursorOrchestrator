diff --git a/README.md b/README.md
index 7f77063..230c91f 100644
--- a/README.md
+++ b/README.md
@@ -1,40 +1,164 @@
-# 🧩 CursorOrchestrator
+# Cursor Orchestrator
 
-![TypeScript](https://img.shields.io/badge/TypeScript-Ready-blue) [![MIT License](https://img.shields.io/badge/License-MIT-yellow.svg)](LICENSE) [![Stars](https://img.shields.io/github/stars/perezdap/CursorOrchestrator)](https://github.com/perezdap/CursorOrchestrator/stargazers)
+Windows-first, Cursor SDK-based agent orchestration framework. Break almost any software task into phases, assign work to configurable agent types, enforce acceptance criteria, and produce durable artifacts under `.runs/<run-id>/`.
 
-**Windows-first multi-phase orchestrator for Cursor + AI agents.**
-Reliable, auditable, resumable workflows with safety gates.
+## What it does
 
-[Quick Start](#-quick-start-under-60-seconds) • [Examples](#-examples) • [Docs](docs/)
+- Loads YAML/JSON **workflows** with phases, agents, and acceptance criteria
+- Executes phases in **dependency order** using pluggable **agent runners**
+- Integrates with the **Cursor SDK** for local and cloud execution (without coupling the core to the SDK)
+- Runs **acceptance checks** (shell commands, file checks, agent review, Pester/Vitest parsers, manual approval)
+- **Retries** failed acceptance and optional retry phases
+- Persists **resumable run state** and artifacts for audit and debugging
+- Enforces **command and file policies** for safer automation on Windows
 
-## 🚀 Quick Start (under 60 seconds)
+## Install
 
-1. **Install**
-   ```bash
-   npm install -g cursor-orchestrator
-   ```
+```powershell
+git clone <repo-url> CursorOrchestrator
+cd CursorOrchestrator
+npm install
+npm run build
+```
 
-2. **Init in your repo**
-   ```bash
-   orchestrator init
-   ```
+Set your Cursor API key for live agent runs:
 
-3. **Run a demo**
-   ```bash
-   orchestrator run --example full-feature
-   # or
-   orchestrator validate
-   orchestrator run
-   ```
+```powershell
+$env:CURSOR_API_KEY = "cursor_..."
+```
 
-**Requirements:** Node 20+, PowerShell 7+, Git repo, Cursor
+## Initialize
 
-Full setup → [docs/quickstart.md](docs/)
+```powershell
+npx orchestrator init
+# or after linking:
+orchestrator init
+```
 
-## 📋 Examples
-(Full list with descriptions in the Examples section below)
+Creates `.orchestrator/config.yaml`, `.orchestrator/README.md`, `workflows/generic-task.workflow.yaml`, and `.runs/`.
 
-More coming soon!
+Run from the **repository root**, not from inside `.orchestrator/`.
 
-## What is it?
-... (keep other sections)
+## Create a workflow
+
+`init` seeds one starter template. To add workflows for your project:
+
+1. Copy the starter and rename it:
+
+```powershell
+Copy-Item .\workflows\generic-task.workflow.yaml .\workflows\my-task.workflow.yaml
+```
+
+2. Edit agents, phases, dependencies, and acceptance criteria in the new file.
+
+3. Validate before running:
+
+```powershell
+orchestrator validate --workflow .\workflows\my-task.workflow.yaml
+```
+
+See [docs/workflows.md](docs/workflows.md) for the full schema. The orchestrator package also ships example workflows you can copy from `node_modules/cursor-orchestrator/src/examples/` (for example `winget-psadt-package.workflow.yaml` for Windows packaging).
+
+## Run a workflow
+
+```powershell
+orchestrator validate --workflow .\src\examples\generic-task.workflow.yaml
+
+orchestrator run `
+  --workflow .\src\examples\generic-task.workflow.yaml `
+  --task "Add unit tests" `
+  --repo-path .
+```
+
+Progress lines print to stderr by default (`[orchestrator] [1/4] Phase intake … running`). Use `--quiet` to suppress them.
+
+Resume a run:
+
+```powershell
+orchestrator resume --run-id <id> --repo-path .
+```
+
+List built-in agent types:
+
+```powershell
+orchestrator list-agents
+```
+
+## Run artifacts
+
+Each run creates:
+
+```text
+.runs/<run-id>/
+  request.md
+  workflow.yaml
+  state.json
+  phase-log.md
+  agent-messages/
+  artifacts/
+  acceptance-report.json
+  acceptance-report.md
+  final-report.md
+```
+
+## Add a new agent type
+
+1. Create `src/agents/my-agent.agent.ts` exporting an `AgentTypeModule`
+2. Register it in `src/agents/index.ts`
+3. Reference the type in workflow YAML under `agents.<id>.type`
+
+See [docs/agents.md](docs/agents.md).
+
+## Add a new acceptance check
+
+Extend `acceptanceCheckSchema` in `src/schemas/acceptance.schema.ts` and add a handler in `AcceptanceRunner.runSingleCheck`.
+
+See [docs/acceptance-criteria.md](docs/acceptance-criteria.md).
+
+## Cursor SDK integration
+
+The orchestrator depends on the `AgentRunner` interface, not the SDK directly:
+
+```typescript
+import { Orchestrator, MockAgentRunner } from "cursor-orchestrator";
+
+const orchestrator = new Orchestrator({
+  agentRunner: new MockAgentRunner(), // or omit for CursorLocalRunner
+  executionMode: "local",
+});
+```
+
+- `cursorRunnerCore` + `composeAgentPrompt` — shared SDK lifecycle and prompt assembly
+- `CursorLocalRunner` / `CursorCloudRunner` — thin adapters for local `cwd` vs cloud VM
+- `AcceptanceGate` — unified acceptance evaluation with retries
+- `NodeShellRunner` — PowerShell-first shell execution for acceptance checks
+
+See [docs/architecture.md](docs/architecture.md).
+
+## Development
+
+```powershell
+npm test
+npm run build
+npm run dev -- validate --workflow .\src\examples\generic-task.workflow.yaml
+```
+
+## Documentation
+
+- [Getting started](docs/getting-started.md) — first workflow run
+- [AGENTS.md](AGENTS.md) — guide for AI agents and contributors
+- [Contributing](CONTRIBUTING.md) — dev setup and PR workflow
+- [Documentation index](docs/README.md)
+
+**Deep dives**
+
+- [Architecture](docs/architecture.md)
+- [Workflows](docs/workflows.md)
+- [Agents](docs/agents.md)
+- [Acceptance criteria](docs/acceptance-criteria.md)
+- [Windows-first design](docs/windows-first.md)
+- [Domain glossary](CONTEXT.md)
+
+## License
+
+MIT
diff --git a/docs/workflows.md b/docs/workflows.md
index ae4468f..f1f3ac9 100644
--- a/docs/workflows.md
+++ b/docs/workflows.md
@@ -58,6 +58,8 @@ acceptance:
 | File | Use case |
 |------|----------|
 | `generic-task.workflow.yaml` | Plan → implement → review → verify |
+| `bug-fix.workflow.yaml` | Diagnose, fix, test, and verify a bug |
+| `new-react-component.workflow.yaml` | Design, implement, test, and document a React component |
 | `winget-psadt-package.workflow.yaml` | Windows packaging with Pester |
 | `repo-review.workflow.yaml` | Research and review pipeline |
 
diff --git a/package-lock.json b/package-lock.json
index b8ab1e0..9636d6f 100644
--- a/package-lock.json
+++ b/package-lock.json
@@ -745,9 +745,6 @@
         "arm"
       ],
       "dev": true,
-      "libc": [
-        "glibc"
-      ],
       "license": "MIT",
       "optional": true,
       "os": [
@@ -762,9 +759,6 @@
         "arm"
       ],
       "dev": true,
-      "libc": [
-        "musl"
-      ],
       "license": "MIT",
       "optional": true,
       "os": [
@@ -779,9 +773,6 @@
         "arm64"
       ],
       "dev": true,
-      "libc": [
-        "glibc"
-      ],
       "license": "MIT",
       "optional": true,
       "os": [
@@ -796,9 +787,6 @@
         "arm64"
       ],
       "dev": true,
-      "libc": [
-        "musl"
-      ],
       "license": "MIT",
       "optional": true,
       "os": [
@@ -813,9 +801,6 @@
         "loong64"
       ],
       "dev": true,
-      "libc": [
-        "glibc"
-      ],
       "license": "MIT",
       "optional": true,
       "os": [
@@ -830,9 +815,6 @@
         "loong64"
       ],
       "dev": true,
-      "libc": [
-        "musl"
-      ],
       "license": "MIT",
       "optional": true,
       "os": [
@@ -847,9 +829,6 @@
         "ppc64"
       ],
       "dev": true,
-      "libc": [
-        "glibc"
-      ],
       "license": "MIT",
       "optional": true,
       "os": [
@@ -864,9 +843,6 @@
         "ppc64"
       ],
       "dev": true,
-      "libc": [
-        "musl"
-      ],
       "license": "MIT",
       "optional": true,
       "os": [
@@ -881,9 +857,6 @@
         "riscv64"
       ],
       "dev": true,
-      "libc": [
-        "glibc"
-      ],
       "license": "MIT",
       "optional": true,
       "os": [
@@ -898,9 +871,6 @@
         "riscv64"
       ],
       "dev": true,
-      "libc": [
-        "musl"
-      ],
       "license": "MIT",
       "optional": true,
       "os": [
@@ -915,9 +885,6 @@
         "s390x"
       ],
       "dev": true,
-      "libc": [
-        "glibc"
-      ],
       "license": "MIT",
       "optional": true,
       "os": [
@@ -932,9 +899,6 @@
         "x64"
       ],
       "dev": true,
-      "libc": [
-        "glibc"
-      ],
       "license": "MIT",
       "optional": true,
       "os": [
@@ -949,9 +913,6 @@
         "x64"
       ],
       "dev": true,
-      "libc": [
-        "musl"
-      ],
       "license": "MIT",
       "optional": true,
       "os": [
diff --git a/src/examples/bug-fix.workflow.yaml b/src/examples/bug-fix.workflow.yaml
index 739a94e..e9d9336 100644
--- a/src/examples/bug-fix.workflow.yaml
+++ b/src/examples/bug-fix.workflow.yaml
@@ -60,9 +60,9 @@ acceptance:
   criteria:
     - id: tests-pass
       type: command
-      command: npm test -- --grep "bug ID or description"
+      command: npm test
       required: true
-    - id: fix-applied
+    - id: diagnosis-ready
       type: markdown_artifact
       path: diagnosis.md
       required: true
diff --git a/src/examples/bugfix-workflow.yaml b/src/examples/bugfix-workflow.yaml
deleted file mode 100644
index abd6647..0000000
--- a/src/examples/bugfix-workflow.yaml
+++ /dev/null
@@ -1,38 +0,0 @@
-name: Intelligent Bug Fix Cycle
-version: 1.0
-
-description: Reproduce, diagnose, fix, test, and verify a bug report.
-
-phases:
-  - name: reproduce
-    agent: qa-engineer
-    task: Reproduce the bug using provided steps or create test case to demonstrate it.
-    acceptance_criteria:
-      - type: shell
-        command: npm test -- --grep bug
-
-  - name: diagnose
-    agent: debugger
-    depends_on: [reproduce]
-    task: Analyze logs, stack traces, and code to identify root cause.
-    acceptance_criteria:
-      - type: file_exists
-        path: docs/root-cause-analysis.md
-
-  - name: fix
-    agent: coder
-    depends_on: [diagnose]
-    task: Implement the minimal fix and add regression test.
-    acceptance_criteria:
-      - type: test_parser
-        framework: vitest
-      - type: shell
-        command: git diff --name-only | grep -q fixed-file
-
-  - name: verify
-    agent: reviewer
-    depends_on: [fix]
-    task: Full verification and approval.
-    acceptance_criteria:
-      - type: manual_approval
-
diff --git a/src/examples/new-react-component.workflow.yaml b/src/examples/new-react-component.workflow.yaml
index 4f762d1..37447ff 100644
--- a/src/examples/new-react-component.workflow.yaml
+++ b/src/examples/new-react-component.workflow.yaml
@@ -24,7 +24,7 @@ agents:
     instructions: |
       Write unit tests, integration tests, and Storybook stories if applicable.
   documenter:
-    type: implementer
+    type: documenter
     model: auto
     instructions: |
       Create or update component documentation and examples.
@@ -60,11 +60,11 @@ acceptance:
   criteria:
     - id: tests-pass
       type: command
-      command: npm test -- --testPathPattern=componentName
+      command: npm test -- --testPathPattern=NewComponent
       required: true
     - id: component-exists
       type: file_exists
-      path: src/components/{componentName}.tsx
+      path: src/components/NewComponent.tsx
       required: true
     - id: storybook-ready
       type: agent_review
diff --git a/src/examples/react-component.workflow.yaml b/src/examples/react-component.workflow.yaml
deleted file mode 100644
index 4fbcc58..0000000
--- a/src/examples/react-component.workflow.yaml
+++ /dev/null
@@ -1,38 +0,0 @@
-name: Implement React Component with Tests
-version: 1.0
-
-phases:
-  - name: requirements-gather
-    agent: analyst
-    task: Analyze the component requirements and create detailed spec including props, states, accessibility, and edge cases.
-    acceptance_criteria:
-      - type: file_exists
-        path: specs/component-spec.md
-      - type: agent_review
-        prompt: Does the spec cover all key aspects?
-
-  - name: implement
-    agent: frontend-coder
-    depends_on: [requirements-gather]
-    task: Implement the React component in TSX with hooks, styles, and full functionality.
-    acceptance_criteria:
-      - type: shell
-        command: npm run typecheck
-      - type: test_parser
-        framework: vitest
-
-  - name: test-and-review
-    agent: qa-reviewer
-    depends_on: [implement]
-    task: Write comprehensive tests and perform code review.
-    acceptance_criteria:
-      - type: manual_approval
-      - type: file_exists
-        path: src/components/NewComponent.test.tsx
-
-output:
-  artifacts:
-    - path: src/components/
-    - path: specs/
-
-# Add to workflows/ via init or copy
